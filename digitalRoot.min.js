/** @format */

function digitalRoot(n) {
  for (; 1 != n.toString().split("").length; ) {
    let sum = n
      .toString()
      .split("")
      .map((item) => parseInt(item, 10))
      .reduce((pre, cur) => pre + cur);
    (n += sum), console.log(n);
  }
}
digitalRoot(12334);
