/** @format */

function duplicateEncode(word) {
  (word = word.toLowerCase()), console.log(word);
  let wordArray = word.split("");
  for (let i = 0; i < wordArray.length; i++) {
    let some = wordArray.indexOf(wordArray[i], i + 1);
    if (((wordArray[i] = -1 == some ? "(" : ")"), i == wordArray.length - 1)) {
      let arr = word.split(""),
        lastElement = arr.splice(wordArray.length - 1, 1),
        result = arr.indexOf(lastElement[0]);
      console.log(arr), (wordArray[wordArray.length - 1] = result ? "(" : ")");
    }
  }
  console.log(wordArray.join(""));
}
duplicateEncode("recede");
